[
  {
    "src": "The backend API sends user metadata. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API отправляет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module validates YAML configuration. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования проверяет YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler sends an HTTP request. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик отправляет HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service indexes CLI output. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации индексирует вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service indexes error events. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации индексирует события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent caches an HTTP request. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга кэширует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser caches the response body. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Парсер кэширует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API validates error events. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API проверяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser serializes user metadata. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер сериализует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process validates error events. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс проверяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer serializes frequently requested data. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой сериализует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API sends database records. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API отправляет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service validates error events. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации проверяет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser stores frequently requested data. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер сохраняет часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer validates CLI output. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой проверяет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service stores JSON documents. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сохраняет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process returns frequently requested data. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс возвращает часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service retries error events. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис повторяет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service serializes frequently requested data. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации сериализует часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler normalizes the response body. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик нормализует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service indexes YAML configuration. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации индексирует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler serializes error events. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик сериализует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module indexes JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования индексирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API stores the response body. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сохраняет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API logs error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API логирует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process caches YAML configuration. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс кэширует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process caches frequently requested data. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс кэширует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler stores JSON documents. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Планировщик сохраняет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process stores YAML configuration. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс сохраняет YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process serializes database records. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс сериализует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module indexes YAML configuration. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования индексирует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser normalizes an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер нормализует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer returns an HTTP request. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой возвращает HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API validates JSON documents. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API проверяет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module stores JSON documents. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сохраняет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer indexes CLI output. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой индексирует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process returns user metadata. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс возвращает метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script indexes JSON documents. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции индексирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service validates frequently requested data. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации проверяет часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process returns CLI output. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс возвращает вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API sends database records. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API отправляет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser validates YAML configuration. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Парсер проверяет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script returns YAML configuration. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции возвращает YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent normalizes database records. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга нормализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service validates the input payload. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации проверяет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик логирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service logs frequently requested data. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис логирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service returns YAML configuration. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации возвращает YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler stores user metadata. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик сохраняет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API normalizes YAML configuration. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API нормализует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process sends the input payload. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс отправляет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler normalizes JSON documents. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик нормализует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service stores error events. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис сохраняет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer normalizes user metadata. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой нормализует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script stores database records. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции сохраняет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service serializes YAML configuration. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сериализует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service sends CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис отправляет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process stores an HTTP request. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс сохраняет HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module validates CLI output. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования проверяет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler validates CLI output. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик проверяет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent sends CLI output. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга отправляет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent serializes YAML configuration. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга сериализует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service retries database records. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации повторяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module caches JSON documents. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования кэширует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service normalizes CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис нормализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API indexes database records. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API индексирует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module retries the input payload. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования повторяет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer stores JSON documents. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой сохраняет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent indexes an HTTP request. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга индексирует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API stores user metadata. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API сохраняет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process stores user metadata. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс сохраняет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service retries database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации повторяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script caches an HTTP request. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции кэширует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer sends an HTTP request. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой отправляет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer stores JSON documents. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой сохраняет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process sends the input payload. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс отправляет входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process sends frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс отправляет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module logs the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования логирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser sends frequently requested data. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер отправляет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API retries YAML configuration. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API повторяет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module returns the response body. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования возвращает тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service stores frequently requested data. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис сохраняет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer stores the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой сохраняет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script returns database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции возвращает записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer returns database records. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой возвращает записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler retries user metadata. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Планировщик повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser indexes error events. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер индексирует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service validates user metadata. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис проверяет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer sends the input payload. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой отправляет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser retries error events. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер повторяет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API logs CLI output. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API логирует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service logs an HTTP request. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис логирует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service retries error events. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации повторяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent retries the input payload. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга повторяет входной payload. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer logs frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой логирует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process stores database records. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс сохраняет записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module serializes CLI output. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования сериализует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process sends user metadata. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс отправляет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler serializes CLI output. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик сериализует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script normalizes database records. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции нормализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module validates user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования проверяет метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser validates database records. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер проверяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service stores an HTTP request. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации сохраняет HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script serializes the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции сериализует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent returns error events. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга возвращает события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service stores an HTTP request. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации сохраняет HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service stores YAML configuration. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации сохраняет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process returns the input payload. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс возвращает входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script indexes YAML configuration. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции индексирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script serializes YAML configuration. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции сериализует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script stores JSON documents. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сохраняет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service caches YAML configuration. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации кэширует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer normalizes the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой нормализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer retries frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой повторяет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module stores database records. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сохраняет записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service validates error events. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации проверяет события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process normalizes error events. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс нормализует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler logs CLI output. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler normalizes an HTTP request. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Планировщик нормализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs YAML configuration. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service normalizes database records. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации нормализует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script sends error events. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции отправляет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module normalizes the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования нормализует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service logs CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис логирует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API logs the response body. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API логирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service caches error events. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис кэширует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script retries frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции повторяет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler caches the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик кэширует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process sends frequently requested data. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс отправляет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service sends database records. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис отправляет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent stores database records. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сохраняет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API serializes database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API сериализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser stores CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сохраняет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module caches the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования кэширует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer serializes error events. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой сериализует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API serializes database records. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API сериализует записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent validates CLI output. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга проверяет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process logs frequently requested data. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс логирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser sends error events. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер отправляет события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler retries JSON documents. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик повторяет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module indexes user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования индексирует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script normalizes error events. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции нормализует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser returns the input payload. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер возвращает входной payload. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service normalizes CLI output. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации нормализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser sends JSON documents. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер отправляет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script returns frequently requested data. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции возвращает часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer serializes user metadata. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой сериализует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process returns the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс возвращает входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script indexes database records. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции индексирует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script sends the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции отправляет входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler returns user metadata. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Планировщик возвращает метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script indexes YAML configuration. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции индексирует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process retries frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс повторяет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer caches database records. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой кэширует записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser validates YAML configuration. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер проверяет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service normalizes user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации нормализует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent caches the input payload. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга кэширует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service retries the response body. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации повторяет тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process stores user metadata. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс сохраняет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process caches the response body. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс кэширует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service serializes user metadata. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис сериализует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script retries the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции повторяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script validates the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции проверяет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API serializes an HTTP request. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API сериализует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent caches the input payload. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга кэширует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process logs CLI output. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс логирует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service indexes error events. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации индексирует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer serializes error events. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сериализует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent sends CLI output. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга отправляет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service caches the input payload. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации кэширует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module indexes the input payload. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования индексирует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler normalizes CLI output. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Планировщик нормализует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service returns user metadata. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис возвращает метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API returns user metadata. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API возвращает метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler validates YAML configuration. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик проверяет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer stores the input payload. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой сохраняет входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer serializes CLI output. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой сериализует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API normalizes user metadata. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API нормализует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process indexes JSON documents. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс индексирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent sends error events. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга отправляет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer sends the input payload. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой отправляет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent sends user metadata. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга отправляет метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module normalizes YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования нормализует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module serializes error events. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования сериализует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script retries error events. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции повторяет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API stores frequently requested data. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API сохраняет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer logs YAML configuration. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой логирует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API retries user metadata. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API повторяет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler indexes an HTTP request. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик индексирует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler sends database records. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Планировщик отправляет записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent normalizes JSON documents. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга нормализует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer indexes the response body. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой индексирует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script validates an HTTP request. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции проверяет HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module caches YAML configuration. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования кэширует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler validates database records. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик проверяет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API stores JSON documents. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сохраняет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer logs JSON documents. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой логирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script indexes the response body. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции индексирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent indexes the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга индексирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler retries YAML configuration. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик повторяет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer caches CLI output. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой кэширует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module normalizes YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования нормализует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser indexes YAML configuration. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser returns database records. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер возвращает записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service logs an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации логирует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler logs the response body. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик логирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module indexes CLI output. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования индексирует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script validates YAML configuration. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции проверяет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module logs the response body. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования логирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer retries CLI output. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой повторяет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser indexes error events. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер индексирует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process sends CLI output. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс отправляет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer returns the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой возвращает входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API normalizes the response body. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API нормализует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service retries an HTTP request. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис повторяет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser logs the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер логирует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler serializes the response body. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик сериализует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API serializes error events. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API сериализует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script serializes the response body. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции сериализует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer normalizes user metadata. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой нормализует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer indexes the response body. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой индексирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser serializes user metadata. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер сериализует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script caches database records. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции кэширует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API returns error events. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API возвращает события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent caches user metadata. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга кэширует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer sends CLI output. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой отправляет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script caches database records. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции кэширует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service returns CLI output. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации возвращает вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API sends user metadata. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API отправляет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent retries the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга повторяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser logs the response body. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер логирует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process retries YAML configuration. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс повторяет YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module caches JSON documents. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования кэширует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API normalizes the input payload. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API нормализует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler indexes frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service caches YAML configuration. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис кэширует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser caches CLI output. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер кэширует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API returns JSON documents. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API возвращает JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process retries CLI output. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс повторяет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script caches error events. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции кэширует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service sends user metadata. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис отправляет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API returns YAML configuration. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API возвращает YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script retries YAML configuration. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции повторяет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module logs the input payload. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования логирует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler caches an HTTP request. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик кэширует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script normalizes database records. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции нормализует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser indexes frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer returns the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой возвращает входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service serializes an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сериализует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service stores CLI output. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации сохраняет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process returns CLI output. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс возвращает вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser stores CLI output. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер сохраняет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module stores YAML configuration. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сохраняет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API sends the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API отправляет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service serializes frequently requested data. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации сериализует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer sends the response body. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой отправляет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API validates JSON documents. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API проверяет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process serializes the input payload. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс сериализует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service serializes an HTTP request. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сериализует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script sends an HTTP request. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции отправляет HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler validates JSON documents. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик проверяет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API retries CLI output. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API повторяет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module caches CLI output. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования кэширует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser logs CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер логирует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script indexes CLI output. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции индексирует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API indexes database records. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API индексирует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service logs frequently requested data. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации логирует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler validates frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Планировщик проверяет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer retries the response body. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой повторяет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API validates the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API проверяет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser stores YAML configuration. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер сохраняет YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process indexes frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser logs an HTTP request. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер логирует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer retries the input payload. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой повторяет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service stores JSON documents. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации сохраняет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser indexes frequently requested data. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent returns error events. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга возвращает события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent indexes an HTTP request. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга индексирует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service stores user metadata. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сохраняет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser sends the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер отправляет тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler validates the response body. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик проверяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process returns error events. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс возвращает события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser stores frequently requested data. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Парсер сохраняет часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent serializes the input payload. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга сериализует входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module serializes an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования сериализует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script logs JSON documents. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции логирует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler caches database records. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик кэширует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser logs the input payload. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер логирует входной payload. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script indexes user metadata. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции индексирует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler serializes an HTTP request. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик сериализует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer returns the response body. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой возвращает тело ответа. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser validates an HTTP request. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер проверяет HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module stores error events. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сохраняет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process indexes frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service logs an HTTP request. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис логирует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser serializes error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер сериализует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler caches the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик кэширует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser stores CLI output. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер сохраняет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API logs the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API логирует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent logs YAML configuration. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга логирует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer retries database records. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой повторяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API indexes the input payload. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API индексирует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service retries frequently requested data. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации повторяет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API stores user metadata. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API сохраняет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent stores the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сохраняет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script serializes CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции сериализует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent retries frequently requested data. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга повторяет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API normalizes JSON documents. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API нормализует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent sends frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга отправляет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer normalizes JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой нормализует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser caches the input payload. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер кэширует входной payload. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent indexes error events. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга индексирует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process sends JSON documents. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс отправляет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser serializes JSON documents. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер сериализует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service normalizes the input payload. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис нормализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process stores an HTTP request. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс сохраняет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script indexes YAML configuration. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции индексирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser retries user metadata. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер повторяет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser serializes an HTTP request. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сериализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser validates YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер проверяет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer serializes the response body. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой сериализует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent stores frequently requested data. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сохраняет часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module serializes the input payload. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования сериализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler retries the response body. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик повторяет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler indexes frequently requested data. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Планировщик индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer stores database records. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой сохраняет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service returns the response body. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации возвращает тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer validates YAML configuration. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой проверяет YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service retries CLI output. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис повторяет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler stores the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик сохраняет тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module normalizes an HTTP request. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования нормализует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service normalizes CLI output. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации нормализует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service indexes database records. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации индексирует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service retries CLI output. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации повторяет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer logs YAML configuration. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой логирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module returns CLI output. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования возвращает вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service normalizes frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис нормализует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process validates user metadata. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс проверяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer normalizes YAML configuration. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой нормализует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API returns JSON documents. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API возвращает JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler sends error events. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Планировщик отправляет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service logs the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации логирует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service retries the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис повторяет входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module sends user metadata. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования отправляет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script normalizes CLI output. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции нормализует вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service caches database records. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис кэширует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent indexes error events. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга индексирует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service caches the input payload. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации кэширует входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler serializes JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик сериализует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script serializes YAML configuration. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции сериализует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent returns the response body. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга возвращает тело ответа. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service serializes frequently requested data. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис сериализует часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler stores the input payload. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Планировщик сохраняет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API indexes YAML configuration. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API индексирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process sends error events. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс отправляет события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler retries user metadata. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Планировщик повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API retries database records. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API повторяет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser stores an HTTP request. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер сохраняет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser stores JSON documents. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер сохраняет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module validates JSON documents. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования проверяет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service sends database records. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации отправляет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer indexes error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой индексирует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer sends database records. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой отправляет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service retries user metadata. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs JSON documents. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Планировщик логирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module normalizes JSON documents. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования нормализует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer validates error events. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой проверяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent stores YAML configuration. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга сохраняет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API validates frequently requested data. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API проверяет часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script serializes error events. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции сериализует события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser normalizes an HTTP request. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер нормализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler caches an HTTP request. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик кэширует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser indexes the response body. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер индексирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent normalizes an HTTP request. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга нормализует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service retries frequently requested data. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации повторяет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service serializes the response body. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сериализует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser normalizes the input payload. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер нормализует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process retries error events. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс повторяет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service validates an HTTP request. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации проверяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process logs YAML configuration. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс логирует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer normalizes database records. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой нормализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script returns the input payload. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции возвращает входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script stores JSON documents. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сохраняет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser validates YAML configuration. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер проверяет YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer stores the response body. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет тело ответа. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service validates database records. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации проверяет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script sends frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции отправляет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser normalizes YAML configuration. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер нормализует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API validates user metadata. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API проверяет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process logs error events. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс логирует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module retries the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования повторяет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process retries the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс повторяет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module stores error events. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования сохраняет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API indexes the response body. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API индексирует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module retries database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования повторяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer validates user metadata. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой проверяет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer stores user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API indexes the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API индексирует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module stores CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования сохраняет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent sends an HTTP request. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга отправляет HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent caches database records. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга кэширует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent sends JSON documents. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга отправляет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer normalizes database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой нормализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script validates an HTTP request. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции проверяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler normalizes JSON documents. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Планировщик нормализует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent stores frequently requested data. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга сохраняет часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler stores frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик сохраняет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API retries database records. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API повторяет записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer normalizes database records. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой нормализует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service caches error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис кэширует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service stores frequently requested data. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сохраняет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service stores JSON documents. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации сохраняет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script caches JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции кэширует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler indexes JSON documents. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик индексирует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent retries an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга повторяет HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script serializes JSON documents. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции сериализует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service validates error events. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис проверяет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser logs frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер логирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service serializes YAML configuration. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сериализует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process caches the input payload. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс кэширует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script indexes user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции индексирует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API stores the input payload. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API сохраняет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser indexes frequently requested data. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер индексирует часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent indexes error events. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга индексирует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service sends the response body. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис отправляет тело ответа. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process normalizes the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс нормализует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module retries CLI output. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования повторяет вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler serializes user metadata. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Планировщик сериализует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process indexes YAML configuration. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer normalizes an HTTP request. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой нормализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API logs CLI output. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API логирует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent normalizes user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга нормализует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service sends CLI output. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации отправляет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process serializes error events. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс сериализует события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service serializes the input payload. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис сериализует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script retries error events. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции повторяет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service logs YAML configuration. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации логирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service stores YAML configuration. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис сохраняет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser normalizes YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер нормализует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module normalizes YAML configuration. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования нормализует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser caches CLI output. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер кэширует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service caches YAML configuration. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации кэширует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer caches database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой кэширует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script retries YAML configuration. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции повторяет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API serializes YAML configuration. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API сериализует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser stores user metadata. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер сохраняет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API retries JSON documents. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API повторяет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service returns YAML configuration. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации возвращает YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer stores the input payload. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service returns an HTTP request. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации возвращает HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service sends the input payload. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации отправляет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script validates the response body. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции проверяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module logs JSON documents. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования логирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer retries user metadata. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent returns YAML configuration. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга возвращает YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler stores an HTTP request. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик сохраняет HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module caches an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования кэширует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent serializes the input payload. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга сериализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script indexes YAML configuration. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process normalizes user metadata. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс нормализует метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser stores JSON documents. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер сохраняет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service retries user metadata. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис повторяет метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module serializes error events. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования сериализует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser validates error events. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер проверяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module normalizes frequently requested data. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования нормализует часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer logs database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой логирует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser caches CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер кэширует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer retries database records. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой повторяет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script caches the response body. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции кэширует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer validates user metadata. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой проверяет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service returns the response body. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации возвращает тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script serializes frequently requested data. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции сериализует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script indexes database records. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции индексирует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API indexes JSON documents. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API индексирует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API serializes the input payload. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сериализует входной payload. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process returns CLI output. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс возвращает вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module retries YAML configuration. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования повторяет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service serializes JSON documents. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации сериализует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script sends JSON documents. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции отправляет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler normalizes CLI output. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик нормализует вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script caches an HTTP request. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции кэширует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service returns CLI output. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис возвращает вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler retries the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик повторяет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API normalizes an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API нормализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module stores error events. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования сохраняет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script logs error events. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции логирует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module retries the input payload. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования повторяет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler indexes the response body. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик индексирует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service caches JSON documents. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис кэширует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process logs database records. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс логирует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser stores user metadata. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сохраняет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service logs CLI output. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис логирует вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service validates the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации проверяет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script serializes an HTTP request. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции сериализует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler normalizes database records. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик нормализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service stores YAML configuration. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис сохраняет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent retries CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга повторяет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service caches the response body. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации кэширует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler caches user metadata. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик кэширует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent stores error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сохраняет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service retries error events. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис повторяет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs the response body. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик логирует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module indexes JSON documents. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования индексирует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer stores JSON documents. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler logs YAML configuration. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик логирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler returns user metadata. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Планировщик возвращает метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API normalizes frequently requested data. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API нормализует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service sends user metadata. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации отправляет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent sends the input payload. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга отправляет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process returns JSON documents. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс возвращает JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer sends database records. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой отправляет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script returns CLI output. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции возвращает вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service indexes frequently requested data. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации индексирует часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script indexes frequently requested data. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции индексирует часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser indexes JSON documents. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер индексирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script retries database records. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции повторяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler sends CLI output. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик отправляет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service caches YAML configuration. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис кэширует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler caches the response body. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Планировщик кэширует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service logs CLI output. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации логирует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process retries user metadata. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс повторяет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script stores frequently requested data. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции сохраняет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process normalizes the response body. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс нормализует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process sends the input payload. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс отправляет входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler stores database records. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик сохраняет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service returns YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис возвращает YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service serializes CLI output. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис сериализует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service sends YAML configuration. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис отправляет YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service sends YAML configuration. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис отправляет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer sends YAML configuration. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой отправляет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API validates YAML configuration. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API проверяет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service serializes YAML configuration. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сериализует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer logs the input payload. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой логирует входной payload. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script serializes database records. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сериализует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API sends the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API отправляет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler indexes JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик индексирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module validates JSON documents. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования проверяет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent validates YAML configuration. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга проверяет YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service serializes JSON documents. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации сериализует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API caches YAML configuration. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API кэширует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module retries an HTTP request. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования повторяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module logs the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования логирует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent returns CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга возвращает вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser logs the response body. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер логирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser sends the input payload. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер отправляет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API retries CLI output. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API повторяет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler retries an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик повторяет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script returns frequently requested data. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции возвращает часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler stores CLI output. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик сохраняет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent caches database records. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга кэширует записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer stores error events. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой сохраняет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser retries the input payload. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер повторяет входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer normalizes frequently requested data. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой нормализует часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module validates the response body. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования проверяет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process indexes YAML configuration. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service validates frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис проверяет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent serializes frequently requested data. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга сериализует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer logs error events. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой логирует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service serializes YAML configuration. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации сериализует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module stores user metadata. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования сохраняет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script stores the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции сохраняет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer logs user metadata. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой логирует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser caches database records. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер кэширует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent returns JSON documents. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга возвращает JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer sends error events. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой отправляет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler retries JSON documents. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик повторяет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent stores user metadata. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга сохраняет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service normalizes frequently requested data. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации нормализует часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser caches JSON documents. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер кэширует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser logs user metadata. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер логирует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API serializes database records. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сериализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module indexes an HTTP request. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования индексирует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script caches the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции кэширует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module caches database records. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования кэширует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler logs the input payload. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script serializes error events. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции сериализует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler indexes an HTTP request. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик индексирует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent validates the input payload. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга проверяет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module serializes an HTTP request. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования сериализует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser logs the input payload. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер логирует входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer returns YAML configuration. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой возвращает YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent serializes database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга сериализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler normalizes CLI output. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик нормализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser sends JSON documents. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер отправляет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler validates the input payload. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Планировщик проверяет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer retries JSON documents. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой повторяет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script retries JSON documents. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции повторяет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script retries the input payload. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции повторяет входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API sends frequently requested data. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API отправляет часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service indexes the response body. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации индексирует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser indexes error events. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер индексирует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler stores the response body. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик сохраняет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service serializes database records. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис сериализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler logs an HTTP request. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик логирует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer validates CLI output. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой проверяет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module retries frequently requested data. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования повторяет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API caches JSON documents. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API кэширует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent logs CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга логирует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API retries frequently requested data. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API повторяет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module caches CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования кэширует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module retries the input payload. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования повторяет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API stores frequently requested data. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сохраняет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler logs CLI output. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler logs the response body. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module indexes the response body. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования индексирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module stores frequently requested data. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования сохраняет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service retries an HTTP request. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис повторяет HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service logs YAML configuration. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации логирует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler normalizes error events. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик нормализует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module validates CLI output. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования проверяет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer sends user metadata. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой отправляет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer stores JSON documents. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой сохраняет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process indexes the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс индексирует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer indexes frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой индексирует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script indexes database records. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции индексирует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service caches an HTTP request. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации кэширует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser validates JSON documents. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер проверяет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service serializes frequently requested data. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации сериализует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module serializes the response body. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сериализует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service serializes frequently requested data. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сериализует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent indexes the response body. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга индексирует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer serializes user metadata. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой сериализует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module logs an HTTP request. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования логирует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module returns the response body. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования возвращает тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler normalizes the response body. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Планировщик нормализует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик логирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler indexes user metadata. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик индексирует метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script retries CLI output. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции повторяет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script retries the input payload. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции повторяет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service logs database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации логирует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser caches frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер кэширует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module serializes JSON documents. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования сериализует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service validates JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис проверяет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module normalizes JSON documents. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования нормализует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service stores user metadata. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сохраняет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API retries user metadata. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module sends error events. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования отправляет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler validates the input payload. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик проверяет входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script validates the response body. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции проверяет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process normalizes an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс нормализует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API logs the response body. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API логирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service logs the input payload. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации логирует входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser retries user metadata. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер повторяет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module stores the input payload. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования сохраняет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service serializes the response body. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сериализует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service stores error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации сохраняет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service validates the input payload. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации проверяет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent logs frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга логирует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module returns user metadata. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования возвращает метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process serializes CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс сериализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service caches database records. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис кэширует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process returns error events. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс возвращает события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service normalizes error events. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации нормализует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script serializes the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции сериализует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser retries the response body. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер повторяет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler retries YAML configuration. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик повторяет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API sends CLI output. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API отправляет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer returns an HTTP request. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой возвращает HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process caches YAML configuration. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс кэширует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler validates YAML configuration. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик проверяет YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module caches the input payload. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования кэширует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process returns the response body. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс возвращает тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler serializes the input payload. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик сериализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service sends database records. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации отправляет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API normalizes CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API нормализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer stores the input payload. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой сохраняет входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service stores JSON documents. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис сохраняет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process caches the response body. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс кэширует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer validates an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой проверяет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer indexes the input payload. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой индексирует входной payload. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API normalizes JSON documents. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API нормализует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service normalizes CLI output. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис нормализует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module caches the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования кэширует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service indexes an HTTP request. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации индексирует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service sends JSON documents. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации отправляет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module stores an HTTP request. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования сохраняет HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service retries database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации повторяет записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer validates CLI output. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой проверяет вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process sends the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс отправляет тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service logs the input payload. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис логирует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer retries the input payload. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой повторяет входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service retries database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис повторяет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer serializes database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой сериализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent normalizes database records. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга нормализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module sends error events. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования отправляет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process retries an HTTP request. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс повторяет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler caches the input payload. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик кэширует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service logs JSON documents. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации логирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer validates an HTTP request. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой проверяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler retries JSON documents. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик повторяет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API indexes the response body. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API индексирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent serializes an HTTP request. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга сериализует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service caches user metadata. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации кэширует метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser retries JSON documents. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер повторяет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API returns the response body. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API возвращает тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module validates the response body. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования проверяет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser logs the input payload. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер логирует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service normalizes user metadata. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации нормализует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API validates the response body. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API проверяет тело ответа. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service returns frequently requested data. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис возвращает часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service retries error events. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации повторяет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer caches the response body. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой кэширует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script indexes YAML configuration. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции индексирует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script validates the response body. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции проверяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service returns the response body. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис возвращает тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser validates CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер проверяет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API sends the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API отправляет входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module validates frequently requested data. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования проверяет часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script sends the response body. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции отправляет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler normalizes an HTTP request. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик нормализует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module retries database records. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования повторяет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer normalizes the response body. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой нормализует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent serializes database records. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сериализует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler indexes JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик индексирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer logs the response body. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой логирует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process returns YAML configuration. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс возвращает YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser normalizes YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Парсер нормализует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent indexes frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга индексирует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module sends frequently requested data. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования отправляет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service caches an HTTP request. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис кэширует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser serializes database records. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сериализует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service returns user metadata. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации возвращает метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service serializes error events. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации сериализует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module returns CLI output. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования возвращает вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent sends frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга отправляет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module serializes database records. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования сериализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer stores JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой сохраняет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service returns CLI output. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис возвращает вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process normalizes an HTTP request. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс нормализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service stores frequently requested data. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сохраняет часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service sends the response body. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис отправляет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API indexes frequently requested data. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API индексирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler serializes the input payload. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Планировщик сериализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser stores the response body. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер сохраняет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser stores YAML configuration. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер сохраняет YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service serializes the response body. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сериализует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer stores the response body. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler serializes an HTTP request. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик сериализует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser serializes database records. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сериализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script stores frequently requested data. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции сохраняет часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser retries the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер повторяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script sends user metadata. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции отправляет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API retries user metadata. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service logs JSON documents. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис логирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler normalizes error events. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик нормализует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler validates JSON documents. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик проверяет JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module normalizes the input payload. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования нормализует входной payload. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service retries user metadata. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser stores error events. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сохраняет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process validates frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс проверяет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script sends error events. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции отправляет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler stores an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Планировщик сохраняет HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service returns error events. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации возвращает события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer caches user metadata. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой кэширует метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service logs CLI output. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис логирует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module sends frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования отправляет часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API caches CLI output. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API кэширует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API sends database records. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API отправляет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API normalizes the input payload. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API нормализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script normalizes user metadata. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции нормализует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler stores frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Планировщик сохраняет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module normalizes an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования нормализует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script caches database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции кэширует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service stores the input payload. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сохраняет входной payload. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process logs CLI output. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс логирует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service logs YAML configuration. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис логирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script serializes database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сериализует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API logs an HTTP request. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API логирует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process serializes CLI output. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс сериализует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script stores the response body. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции сохраняет тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module sends error events. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования отправляет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service stores CLI output. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации сохраняет вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API validates error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API проверяет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The worker process retries user metadata. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс повторяет метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service serializes CLI output. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис сериализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API logs the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API логирует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent serializes the input payload. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга сериализует входной payload. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API caches YAML configuration. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API кэширует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer logs JSON documents. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой логирует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script sends the response body. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции отправляет тело ответа. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module serializes CLI output. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования сериализует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler retries database records. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик повторяет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer retries an HTTP request. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой повторяет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer caches JSON documents. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой кэширует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module caches JSON documents. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования кэширует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler serializes the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик сериализует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service logs JSON documents. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации логирует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The frontend service indexes the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис индексирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process logs the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс логирует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service stores database records. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис сохраняет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service sends JSON documents. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации отправляет JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service caches frequently requested data. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации кэширует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler validates an HTTP request. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Планировщик проверяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script serializes JSON documents. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции сериализует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent indexes frequently requested data. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга индексирует часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer validates user metadata. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой проверяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service stores CLI output. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис сохраняет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service returns user metadata. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации возвращает метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service sends JSON documents. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис отправляет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent sends database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга отправляет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer caches error events. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой кэширует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script caches database records. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции кэширует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process serializes error events. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс сериализует события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service indexes frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис индексирует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent caches user metadata. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга кэширует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API retries user metadata. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API повторяет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer caches database records. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой кэширует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service sends CLI output. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации отправляет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module sends JSON documents. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования отправляет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs error events. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует события ошибок. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent returns frequently requested data. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга возвращает часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script retries YAML configuration. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции повторяет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process stores user metadata. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс сохраняет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module caches YAML configuration. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования кэширует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API returns frequently requested data. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API возвращает часто запрашиваемые данные. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler retries frequently requested data. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик повторяет часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer normalizes the input payload. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой нормализует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent serializes CLI output. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сериализует вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service indexes the input payload. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации индексирует входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer logs an HTTP request. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой логирует HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service retries error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации повторяет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module retries the response body. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования повторяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser stores frequently requested data. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер сохраняет часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script caches database records. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции кэширует записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module normalizes the input payload. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования нормализует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler logs error events. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик логирует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler sends an HTTP request. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик отправляет HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser sends frequently requested data. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер отправляет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service sends database records. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис отправляет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer stores the input payload. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent caches the response body. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга кэширует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer caches JSON documents. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой кэширует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process retries error events. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс повторяет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script retries database records. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции повторяет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service returns database records. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис возвращает записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service serializes YAML configuration. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис сериализует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser sends error events. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер отправляет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent sends CLI output. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга отправляет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service sends the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис отправляет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler validates YAML configuration. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик проверяет YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module returns an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования возвращает HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API indexes user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API индексирует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module logs the input payload. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования логирует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API caches the input payload. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API кэширует входной payload. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service serializes database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации сериализует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service retries error events. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации повторяет события ошибок. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service indexes JSON documents. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис индексирует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser returns an HTTP request. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер возвращает HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API serializes JSON documents. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API сериализует JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process validates the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс проверяет входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script caches user metadata. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции кэширует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API stores an HTTP request. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API сохраняет HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser logs the input payload. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер логирует входной payload. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service stores the response body. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Фронтенд-сервис сохраняет тело ответа. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script serializes user metadata. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сериализует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser stores the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер сохраняет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script caches error events. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции кэширует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module validates the response body. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования проверяет тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API logs CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API логирует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script stores CLI output. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сохраняет вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent sends the response body. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга отправляет тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API logs JSON documents. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API логирует JSON-документы. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer retries an HTTP request. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой повторяет HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent returns frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга возвращает часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent validates error events. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга проверяет события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser logs the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер логирует тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service sends the response body. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис отправляет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script stores the input payload. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции сохраняет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service retries user metadata. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации повторяет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service indexes error events. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации индексирует события ошибок. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API normalizes JSON documents. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API нормализует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API normalizes JSON documents. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API нормализует JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The migration script sends the input payload. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции отправляет входной payload. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service indexes YAML configuration. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис индексирует YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent serializes user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга сериализует метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The authentication service normalizes frequently requested data. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации нормализует часто запрашиваемые данные. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service normalizes frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации нормализует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent normalizes frequently requested data. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга нормализует часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API returns an HTTP request. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API возвращает HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer indexes YAML configuration. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой индексирует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service stores JSON documents. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис сохраняет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API caches CLI output. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API кэширует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API logs database records. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API логирует записи базы данных. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser validates error events. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер проверяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API normalizes JSON documents. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API нормализует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module normalizes an HTTP request. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования нормализует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process logs CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс логирует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module serializes the response body. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования сериализует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler serializes CLI output. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик сериализует вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler logs CLI output. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Планировщик логирует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler retries user metadata. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик повторяет метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module logs CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Модуль логирования логирует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process retries YAML configuration. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс повторяет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service serializes user metadata. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации сериализует метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module indexes YAML configuration. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования индексирует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser indexes JSON documents. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер индексирует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler indexes user metadata. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик индексирует метаданные пользователя. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process validates an HTTP request. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс проверяет HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service sends database records. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис отправляет записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer caches the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой кэширует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service indexes an HTTP request. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации индексирует HTTP-запрос. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API serializes YAML configuration. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сериализует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler returns YAML configuration. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик возвращает YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler logs error events. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик логирует события ошибок. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service sends user metadata. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис отправляет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script returns frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции возвращает часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent returns database records. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга возвращает записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module validates the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Модуль логирования проверяет тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API logs an HTTP request. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API логирует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process caches the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс кэширует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer sends frequently requested data. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой отправляет часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service stores frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис сохраняет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent caches JSON documents. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга кэширует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser validates frequently requested data. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер проверяет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module indexes the input payload. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования индексирует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler indexes YAML configuration. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The parser returns database records. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Парсер возвращает записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser caches database records. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер кэширует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script indexes an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции индексирует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script returns CLI output. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции возвращает вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service retries error events. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис повторяет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The cache layer indexes user metadata. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой индексирует метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The parser indexes YAML configuration. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Парсер индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The frontend service caches database records. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис кэширует записи базы данных. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API validates the response body. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API проверяет тело ответа. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service validates database records. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис проверяет записи базы данных. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The backend API returns YAML configuration. If a timeout occurs, it retries the request three times. Use `python src/train.py` to start fine-tuning.",
    "reference": "Бэкенд API возвращает YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module sends JSON documents. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования отправляет JSON-документы. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API validates an HTTP request. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API проверяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The worker process validates CLI output. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс проверяет вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler logs JSON documents. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Планировщик логирует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser sends error events. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Парсер отправляет события ошибок. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service serializes the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Фронтенд-сервис сериализует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The backend API retries YAML configuration. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Бэкенд API повторяет YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The cache layer serializes the input payload. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой сериализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The scheduler logs an HTTP request. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик логирует HTTP-запрос. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process returns the response body. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Воркер-процесс возвращает тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer stores user metadata. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Кэш-слой сохраняет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process indexes the response body. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс индексирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service serializes CLI output. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис сериализует вывод CLI. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process validates user metadata. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Воркер-процесс проверяет метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service logs YAML configuration. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации логирует YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module returns CLI output. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования возвращает вывод CLI. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API stores CLI output. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API сохраняет вывод CLI. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The migration script indexes the input payload. If access is denied, it returns status code 403. Use `python src/train.py` to start fine-tuning.",
    "reference": "Скрипт миграции индексирует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer validates database records. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Кэш-слой проверяет записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser retries the response body. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер повторяет тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The cache layer serializes the input payload. If authentication fails, it returns status code 401. Config path: `configs/train_config.json`.",
    "reference": "Кэш-слой сериализует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The migration script normalizes frequently requested data. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции нормализует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The monitoring agent stores CLI output. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Агент мониторинга сохраняет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser logs the response body. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Парсер логирует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The authentication service returns error events. If access is denied, it returns status code 403. Config path: `configs/train_config.json`.",
    "reference": "Сервис аутентификации возвращает события ошибок. Если доступ запрещён, он возвращает код состояния 403. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module retries JSON documents. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования повторяет JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser caches JSON documents. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер кэширует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The logging module serializes the input payload. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сериализует входной payload. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The worker process returns user metadata. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Воркер-процесс возвращает метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service indexes an HTTP request. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Фронтенд-сервис индексирует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler serializes database records. If parameters are invalid, it returns status code 400. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Планировщик сериализует записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The cache layer indexes YAML configuration. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Кэш-слой индексирует YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser logs database records. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер логирует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The logging module normalizes YAML configuration. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования нормализует YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler retries YAML configuration. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик повторяет YAML-конфигурацию. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent stores YAML configuration. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Агент мониторинга сохраняет YAML-конфигурацию. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module stores CLI output. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сохраняет вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script retries an HTTP request. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Скрипт миграции повторяет HTTP-запрос. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service validates user metadata. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Сервис аутентификации проверяет метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The frontend service returns YAML configuration. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис возвращает YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script normalizes an HTTP request. If parameters are invalid, it returns status code 400. Config path: `configs/train_config.json`.",
    "reference": "Скрипт миграции нормализует HTTP-запрос. Если параметры некорректны, он возвращает код состояния 400. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The backend API indexes user metadata. If access is denied, it returns status code 403. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API индексирует метаданные пользователя. Если доступ запрещён, он возвращает код состояния 403. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process serializes the response body. If a timeout occurs, it retries the request three times. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс сериализует тело ответа. Если возникает тайм-аут, он повторяет запрос три раза. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser serializes the input payload. If the resource is missing, it returns status code 404. Config path: `configs/train_config.json`.",
    "reference": "Парсер сериализует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The scheduler returns YAML configuration. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Планировщик возвращает YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  },
  {
    "src": "The parser normalizes JSON documents. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер нормализует JSON-документы. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The worker process stores JSON documents. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Воркер-процесс сохраняет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The backend API indexes user metadata. If authentication fails, it returns status code 401. Use `pip install -r requirements.txt` before execution.",
    "reference": "Бэкенд API индексирует метаданные пользователя. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The logging module stores frequently requested data. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Модуль логирования сохраняет часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service logs frequently requested data. If parameters are invalid, it returns status code 400. Endpoint: `/v1/users/{id}`.",
    "reference": "Сервис аутентификации логирует часто запрашиваемые данные. Если параметры некорректны, он возвращает код состояния 400. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The migration script normalizes database records. If the resource is missing, it returns status code 404. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции нормализует записи базы данных. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The scheduler sends database records. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Планировщик отправляет записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The monitoring agent serializes the response body. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга сериализует тело ответа. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The frontend service serializes the response body. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Фронтенд-сервис сериализует тело ответа. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The cache layer stores YAML configuration. If a timeout occurs, it retries the request three times. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Кэш-слой сохраняет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The monitoring agent sends user metadata. If parameters are invalid, it returns status code 400. Use `pip install -r requirements.txt` before execution.",
    "reference": "Агент мониторинга отправляет метаданные пользователя. Если параметры некорректны, он возвращает код состояния 400. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script serializes JSON documents. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Скрипт миграции сериализует JSON-документы. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The authentication service logs CLI output. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации логирует вывод CLI. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The logging module serializes YAML configuration. If access is denied, it returns status code 403. Use `pip install -r requirements.txt` before execution.",
    "reference": "Модуль логирования сериализует YAML-конфигурацию. Если доступ запрещён, он возвращает код состояния 403. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The parser normalizes CLI output. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Парсер нормализует вывод CLI. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The authentication service logs error events. If authentication fails, it returns status code 401. Use `python src/train.py` to start fine-tuning.",
    "reference": "Сервис аутентификации логирует события ошибок. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The frontend service returns YAML configuration. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Фронтенд-сервис возвращает YAML-конфигурацию. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The logging module serializes the input payload. If authentication fails, it returns status code 401. Endpoint: `/v1/users/{id}`.",
    "reference": "Модуль логирования сериализует входной payload. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The monitoring agent serializes an HTTP request. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Агент мониторинга сериализует HTTP-запрос. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API validates JSON documents. If authentication fails, it returns status code 401. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Бэкенд API проверяет JSON-документы. Если аутентификация завершается ошибкой, он возвращает код состояния 401. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The worker process sends error events. If the resource is missing, it returns status code 404. Endpoint: `/v1/users/{id}`.",
    "reference": "Воркер-процесс отправляет события ошибок. Если ресурс отсутствует, он возвращает код состояния 404. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The parser sends YAML configuration. If a timeout occurs, it retries the request three times. Use `pip install -r requirements.txt` before execution.",
    "reference": "Парсер отправляет YAML-конфигурацию. Если возникает тайм-аут, он повторяет запрос три раза. Используйте `pip install -r requirements.txt` перед выполнением."
  },
  {
    "src": "The migration script logs frequently requested data. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Скрипт миграции логирует часто запрашиваемые данные. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The backend API logs the response body. If access is denied, it returns status code 403. Endpoint: `/v1/users/{id}`.",
    "reference": "Бэкенд API логирует тело ответа. Если доступ запрещён, он возвращает код состояния 403. Эндпоинт: `/v1/users/{id}`."
  },
  {
    "src": "The authentication service validates user metadata. If the resource is missing, it returns status code 404. Payload example: {\"enabled\": true, \"timeout\": 30}.",
    "reference": "Сервис аутентификации проверяет метаданные пользователя. Если ресурс отсутствует, он возвращает код состояния 404. Пример payload: {\"enabled\": true, \"timeout\": 30}."
  },
  {
    "src": "The scheduler caches database records. If parameters are invalid, it returns status code 400. Use `python src/train.py` to start fine-tuning.",
    "reference": "Планировщик кэширует записи базы данных. Если параметры некорректны, он возвращает код состояния 400. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The monitoring agent indexes the input payload. If the resource is missing, it returns status code 404. Use `python src/train.py` to start fine-tuning.",
    "reference": "Агент мониторинга индексирует входной payload. Если ресурс отсутствует, он возвращает код состояния 404. Используйте `python src/train.py`, чтобы запустить дообучение."
  },
  {
    "src": "The scheduler returns frequently requested data. If a timeout occurs, it retries the request three times. Config path: `configs/train_config.json`.",
    "reference": "Планировщик возвращает часто запрашиваемые данные. Если возникает тайм-аут, он повторяет запрос три раза. Путь к конфигу: `configs/train_config.json`."
  }
]